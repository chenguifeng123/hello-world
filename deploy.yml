basic:
  sysCode: testpaas
  sysName: 容器健康检测
  maintainer: 负责人姓名
deploy:
  app: testPaaS.jar
  middleware: weblogic-jdk1.7/weblogic-jdk1.6/tomcat/alpine-java
  replicas: 1
  contextPath: testPaaS
  domain: www.test.paas.com
  targetCluster: kubernetes
  targetHost: 10.134.161.45
  lbType: 4IP/7DOMAIN
  nginx: nginx37
  host: 127.0.0.1 www.baidu.com#127.0.0.2 www.sina.com
  dockerfileCmd: mkdir -p path
  addCmd: ["/bin/echo", "this is a echo test"]
config:
  -name: a.yml
   path:/root/test
  -name: b.properties
   path: /root/testpaas/
datasource:
  -name: ds1
   dbtype: mysql
   url: jdbc:xxxxxx
  -name: ds2
   dbtype: oracle
   url: jdbc:xxxxxx
  